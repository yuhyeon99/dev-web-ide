services:
  redis:
    image: redis:7.4-alpine
    container_name: dev-web-ide-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

volumes:
  redis-data:
